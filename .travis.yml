language: python
python:
  - 3.7
cache:
  pip: true
install:
  - pip install -r requirements.txt
_ahjhj: &shared_jobs
  script: echo "shared script config"
jobs:
  include:
  - name: Job 1
    <<: *shared_jobs
  - name: Job 2
    <<: *shared_jobs